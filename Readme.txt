Progetto di SAW.

Nome:
    MemeBank;

Funzionalità da implementare obbligatoriamente:
   -Presentazione di MemeBank 
   *Registrazione di un utente - sovraimpressione finchè l'utente non è autenticato
   *Login al sistema - sovraimpressione finchè l'utente non è autenticato
   *Visualizzazione del profilo utente - solo se l'utente è autenticato
   -Modifica del profilo utente - solo se l'utente è autenticato
   *Logout - solo se l'utente è autenticato in "Visualizzazione accanto a Modifica profilo"
   -*Motore di ricerca interno - solo se l'utente è autenticato cerca un utente

Funzionalità da implementare a scelta (due):
   Pagina Stream - solo se l'utente è autenticato
   Pubblica immagine - solo se l'utente è autenticato in sovraimpressione sul profilo
   Valutazione prodotti (Like)
   -Carrello per acquisti (Salva post)
   -Newsletter (Notifica per Mail)

Funzionalità in implementazione:
    libreria database;
